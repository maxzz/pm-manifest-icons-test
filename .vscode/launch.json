{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome for app",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}/packages/app",
        },
    ],
}
